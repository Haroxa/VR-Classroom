<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.university.vrclassroombackend.module.forum.mapper.CommentLikeMapper">

    <select id="existsByUserIdAndCommentId" resultType="java.lang.Boolean">
        SELECT COUNT(*) > 0
        FROM comment_like
        WHERE user_id = #{userId} AND comment_id = #{commentId}
    </select>

    <select id="countByCommentId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM comment_like
        WHERE comment_id = #{commentId}
    </select>

    <delete id="deleteByUserIdAndCommentId">
        DELETE FROM comment_like
        WHERE user_id = #{userId} AND comment_id = #{commentId}
    </delete>

    <select id="selectLikedCommentIdsByUserId" resultType="java.lang.Integer">
        SELECT comment_id
        FROM comment_like
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

</mapper>
