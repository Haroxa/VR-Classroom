<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.university.vrclassroombackend.module.forum.mapper.PostLikeMapper">

    <select id="existsByUserIdAndPostId" resultType="java.lang.Boolean">
        SELECT COUNT(*) > 0
        FROM post_like
        WHERE user_id = #{userId} AND post_id = #{postId}
    </select>

    <select id="countByPostId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM post_like
        WHERE post_id = #{postId}
    </select>

    <delete id="deleteByUserIdAndPostId">
        DELETE FROM post_like
        WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

    <select id="selectLikedPostIdsByUserId" resultType="java.lang.Integer">
        SELECT post_id
        FROM post_like
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

</mapper>
