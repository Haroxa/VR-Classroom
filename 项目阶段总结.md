# VR教室后端项目 - 阶段总结

## 项目概述

VR教室后端项目是一个基于Spring Boot 3.5.9开发的RESTful API服务，为VR教室前端应用提供完整的后端支持。项目采用分层架构设计，实现了用户管理、帖子管理、评论管理等核心功能，并集成了阿里云OSS文件存储服务。

## 技术栈

### 后端框架
- **Spring Boot 3.5.9**：核心应用框架
- **Spring Data JPA**：数据持久化层
- **Spring MVC**：Web层框架
- **MySQL 8.0.33**：关系型数据库

### 认证与安全
- **JWT (jjwt 0.11.5)**：基于令牌的认证机制
- **自定义拦截器**：实现统一的认证和授权

### 云服务集成
- **阿里云OSS SDK 3.15.1**：对象存储服务
- **OSS签名生成**：支持前端直传文件

### 开发工具
- **Spring Boot DevTools**：热部署支持
- **Lombok**：简化实体类开发
- **Maven**：项目构建和依赖管理

## 核心功能模块

### 1. 用户管理模块

#### 接口列表
- **POST /api/users** - 创建用户
- **POST /api/users/login** - 用户登录
- **GET /api/users/profile** - 获取用户信息
- **GET /api/users/posts** - 获取用户帖子列表
- **GET /api/users/comments** - 获取用户评论列表

#### 功能特性
- 支持微信OpenID用户注册
- 基于手机号的用户登录
- JWT令牌生成和验证
- 用户信息查询和展示
- 用户内容管理（帖子、评论）

#### 关键代码

**UserController.java - 用户登录接口**
```java
@PostMapping("/login")
public ResponseEntity<?> login(@RequestBody LoginDTO dto) {
    User user = userService.getUserByPhone(dto.getPhone());
    if (user == null) {
        return ResponseEntity.status(HttpStatus.UNAUTHORIZED)
            .body(ApiResponse.error(HttpStatus.UNAUTHORIZED, ErrorMessage.USER_NOT_FOUND));
    }
    
    String token = jwtUtil.generateToken(user.getId());
    
    Map<String, Object> data = new HashMap<>();
    data.put("token", token);
    data.put("user", userService.getUserProfile(user.getId()));
    
    return ResponseEntity.ok().body(ApiResponse.success(data));
}
```

### 2. 帖子管理模块

#### 接口列表
- **GET /api/posts** - 获取公开帖子列表（支持分页、分类、关键词搜索）
- **POST /api/posts** - 创建帖子（需认证）
- **GET /api/posts/{id}** - 获取帖子详情
- **PUT /api/posts/{id}** - 更新帖子（需认证，仅作者）
- **DELETE /api/posts/{id}** - 删除帖子（需认证，仅作者）

#### 功能特性
- 帖子发布和编辑
- 支持分类管理
- 关键词搜索功能
- 分页查询支持
- 帖子状态管理（已发布、待审核、已删除、已驳回）
- 图片上传支持

#### 关键代码

**PostController.java - 创建帖子接口**
```java
@PostMapping
public ResponseEntity<?> createPost(
        jakarta.servlet.http.HttpServletRequest request,
        @RequestBody PostCreateDTO dto) {
    try {
        Integer authorId = (Integer) request.getAttribute(Auth.USER_ID_ATTRIBUTE);
        if (authorId == null) {
            return ResponseEntity.status(HttpStatus.UNAUTHORIZED)
                .body(ApiResponse.error(HttpStatus.UNAUTHORIZED, ErrorMessage.UNAUTHORIZED_USER));
        }
        
        Integer postId = postService.createPost(dto, authorId);
        return ResponseEntity.ok().body(ApiResponse.success(postId.toString()));
    } catch (Exception e) {
        logger.error("创建帖子失败: authorId={}, title={}", 
            request.getAttribute(Auth.USER_ID_ATTRIBUTE), 
            dto != null ? dto.getTitle() : null, e);
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
            .body(ApiResponse.error(HttpStatus.INTERNAL_SERVER_ERROR, ErrorMessage.CREATE_POST_FAILED));
    }
}
```

**PostServiceImpl.java - 帖子创建业务逻辑**
```java
@Override
public Integer createPost(PostCreateDTO dto, Integer authorId) {
    Post post = new Post();
    post.setDate(new SimpleDateFormat("yyyy-MM-dd HH:mm:ss").format(new Date()));
    
    // 确保所有非空字段都有值
    post.setTitle(dto.getTitle() != null ? dto.getTitle() : "");
    String content = dto.getContent() != null ? dto.getContent() : "";
    post.setContent(content);
    String summary = content.length() > Post.SUMMARY_LENGTH ? 
        content.substring(0, Post.SUMMARY_LENGTH) : content;
    post.setSummary(summary);
    post.setImages(dto.getImages());
    post.setCategoryId(dto.getCategoryId() != null ? dto.getCategoryId() : 
        AppConstants.Post.DEFAULT_CATEGORY_ID);
    post.setAuthorId(authorId != null ? authorId : 0);
    post.setLikeCount(0);
    post.setShareCount(0);
    post.setCommentCount(0);
    post.setStatus(Post.STATUS_PUBLISHED);
    
    Post savedPost = postRepository.save(post);
    return savedPost.getId();
}
```

### 3. 评论管理模块

#### 接口列表
- **GET /api/comments** - 获取帖子评论列表（支持分页）
- **POST /api/comments** - 创建评论（需认证）
- **PUT /api/comments/{id}** - 更新评论（需认证，仅作者）
- **DELETE /api/comments/{id}** - 删除评论（需认证，仅作者）

#### 功能特性
- 评论发布和编辑
- 关联帖子查询
- 分页查询支持
- 评论状态管理
- 点赞功能支持

#### 关键代码

**CommentController.java - 创建评论接口**
```java
@PostMapping
public ResponseEntity<?> createComment(
        jakarta.servlet.http.HttpServletRequest request,
        @RequestBody CommentCreateDTO dto) {
    try {
        Integer commenterId = (Integer) request.getAttribute(Auth.USER_ID_ATTRIBUTE);
        if (commenterId == null) {
            return ResponseEntity.status(HttpStatus.UNAUTHORIZED)
                .body(ApiResponse.error(HttpStatus.UNAUTHORIZED, ErrorMessage.UNAUTHORIZED_USER));
        }
        
        Integer commentId = commentService.createComment(dto, commenterId);
        Map<String, Object> data = new HashMap<>();
        data.put("id", commentId);
        return ResponseEntity.ok().body(ApiResponse.success(data));
    } catch (Exception e) {
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
            .body(ApiResponse.error(HttpStatus.INTERNAL_SERVER_ERROR, ErrorMessage.CREATE_COMMENT_FAILED));
    }
}
```

### 4. 公共服务模块

#### 接口列表
- **GET /api/oss/sign** - 获取OSS上传签名（可选认证）
- **GET /health** - 健康检查

#### 功能特性
- 阿里云OSS直传签名生成
- 文件上传目录管理
- 签名过期时间控制（1小时）
- 应用健康状态监控

#### 关键代码

**OssUtil.java - OSS签名生成**
```java
public Map<String, String> generatePostSignature() {
    OSS ossClient = new OSSClientBuilder().build(endpoint, accessKeyId, accessKeySecret);
    
    try {
        // 设置过期时间为1小时
        Date expiration = new Date(System.currentTimeMillis() + 3600 * 1000);
        PolicyConditions policyConds = new PolicyConditions();
        policyConds.addConditionItem(PolicyConditions.COND_CONTENT_LENGTH_RANGE, 0, 1048576000);
        
        String postPolicy = ossClient.generatePostPolicy(expiration, policyConds);
        byte[] binaryData = postPolicy.getBytes("utf-8");
        String encodedPolicy = BinaryUtil.toBase64String(binaryData);
        String postSignature = ossClient.calculatePostSignature(postPolicy);
        
        Map<String, String> respMap = new LinkedHashMap<>();
        respMap.put("accessid", accessKeyId);
        respMap.put("policy", encodedPolicy);
        respMap.put("signature", postSignature);
        respMap.put("dir", "user-uploads/");
        respMap.put("host", "https://" + bucketName + "." + endpoint);
        respMap.put("expire", String.valueOf(expiration.getTime() / 1000));
        
        return respMap;
    } catch (Exception e) {
        e.printStackTrace();
        return null;
    } finally {
        ossClient.shutdown();
    }
}
```

## 安全机制

### JWT认证实现

#### JwtUtil.java - JWT工具类
```java
@Component
public class JwtUtil {
    
    @Value("${jwt.expiration}")
    private Long expiration;
    
    @Value("${jwt.secret}")
    private String secret;
    
    private SecretKey signingKey;
    
    @PostConstruct
    public void init() {
        byte[] keyBytes = secret.getBytes(StandardCharsets.UTF_8);
        if (keyBytes.length < 32) {
            keyBytes = MessageDigest.getInstance("SHA-256").digest(keyBytes);
        }
        this.signingKey = Keys.hmacShaKeyFor(keyBytes);
    }
    
    public String generateToken(Integer userId) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + expiration * 1000);
        
        return Jwts.builder()
                .setSubject(userId.toString())
                .setIssuedAt(now)
                .setExpiration(expiryDate)
                .signWith(getSigningKey())
                .compact();
    }
    
    public Integer getUserIdFromToken(String token) {
        Claims claims = Jwts.parserBuilder()
                .setSigningKey(getSigningKey())
                .build()
                .parseClaimsJws(token)
                .getBody();
        
        return Integer.parseInt(claims.getSubject());
    }
    
    public boolean validateToken(String token) {
        try {
            Jwts.parserBuilder()
                    .setSigningKey(getSigningKey())
                    .build()
                    .parseClaimsJws(token);
            return true;
        } catch (Exception e) {
            return false;
        }
    }
}
```

### 认证拦截器

#### AuthInterceptor.java - 认证拦截器
```java
@Component
public class AuthInterceptor implements HandlerInterceptor {
    
    @Autowired
    private JwtUtil jwtUtil;
    
    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        String path = request.getRequestURI();
        String method = request.getMethod();
        
        // 检查是否为公开路径（无需强制认证）
        if (isPublicPath(path, method)) {
            handleOptionalAuth(request);
            return true;
        }
        
        // 非公开路径需要强制认证
        return handleRequiredAuth(request, response);
    }
    
    private boolean isPublicPath(String path, String method) {
        return (path.equals("/api/posts") && method.equals("GET")) ||
               (path.startsWith("/api/posts/") && method.equals("GET")) ||
               (path.equals("/api/comments") && method.equals("GET")) ||
               (path.equals("/api/oss/sign") && method.equals("GET"));
    }
    
    private boolean handleRequiredAuth(HttpServletRequest request, HttpServletResponse response) {
        String token = request.getHeader("Authorization");
        
        if (token == null || !token.startsWith(Auth.BEARER_PREFIX)) {
            response.setStatus(HttpStatus.UNAUTHORIZED);
            response.setContentType("application/json;charset=UTF-8");
            response.getWriter().write(objectMapper.writeValueAsString(
                ApiResponse.error(HttpStatus.UNAUTHORIZED, ErrorMessage.TOKEN_NOT_PROVIDED)));
            return false;
        }
        
        token = token.substring(Auth.BEARER_PREFIX.length());
        
        if (!jwtUtil.validateToken(token)) {
            response.setStatus(HttpStatus.UNAUTHORIZED);
            response.setContentType("application/json;charset=UTF-8");
            response.getWriter().write(objectMapper.writeValueAsString(
                ApiResponse.error(HttpStatus.UNAUTHORIZED, ErrorMessage.INVALID_TOKEN)));
            return false;
        }
        
        Integer userId = jwtUtil.getUserIdFromToken(token);
        request.setAttribute(Auth.USER_ID_ATTRIBUTE, userId);
        
        return true;
    }
}
```

## 数据库设计

### 数据表结构

#### 用户表 (user)
```sql
CREATE TABLE user (
    id INT PRIMARY KEY AUTO_INCREMENT,
    phone VARCHAR(20) UNIQUE NOT NULL,
    openId VARCHAR(100) UNIQUE NOT NULL,
    name VARCHAR(50) NOT NULL,
    avatar VARCHAR(200) NOT NULL,
    collegeId VARCHAR(10) NOT NULL,
    verifyStatus INT NOT NULL DEFAULT 0
);
```

#### 帖子表 (post)
```sql
CREATE TABLE post (
    id INT PRIMARY KEY AUTO_INCREMENT,
    date VARCHAR(20) NOT NULL,
    title VARCHAR(100) NOT NULL,
    summary VARCHAR(200) NOT NULL,
    content TEXT NOT NULL,
    authorId INT NOT NULL,
    categoryId INT NOT NULL,
    likeCount INT NOT NULL DEFAULT 0,
    shareCount INT NOT NULL DEFAULT 0,
    commentCount INT NOT NULL DEFAULT 0,
    status INT NOT NULL DEFAULT 0,
    rejectReason VARCHAR(200),
    FOREIGN KEY (authorId) REFERENCES user(id),
    FOREIGN KEY (categoryId) REFERENCES category(id)
);
```

#### 评论表 (comment)
```sql
CREATE TABLE comment (
    id INT PRIMARY KEY AUTO_INCREMENT,
    date VARCHAR(20) NOT NULL,
    content TEXT NOT NULL,
    commenterId INT NOT NULL,
    postId INT NOT NULL,
    likeCount INT NOT NULL DEFAULT 0,
    status INT NOT NULL DEFAULT 0,
    rejectReason VARCHAR(200),
    FOREIGN KEY (commenterId) REFERENCES user(id),
    FOREIGN KEY (postId) REFERENCES post(id)
);
```

#### 分类表 (category)
```sql
CREATE TABLE category (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL
);
```

#### 学院表 (college)
```sql
CREATE TABLE college (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL
);
```

## 配置系统

### 环境配置

#### 开发环境 (application-dev.yml)
- 数据库：本地MySQL数据库
- 日志级别：DEBUG
- 热部署：启用
- OSS配置：支持环境变量覆盖

#### 测试环境 (application-test.yml)
- 数据库：独立测试数据库
- 日志级别：INFO
- 热部署：禁用
- 端口：8081

#### 生产环境 (application-prod.yml)
- 数据库：云数据库（通过环境变量配置）
- 日志级别：WARN
- 热部署：禁用
- 安全配置：启用健康检查

### 环境变量管理

#### .env文件配置
```env
# 数据库配置
SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/vr_classroom
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=123456

# OSS配置
OSS_ENDPOINT=oss-cn-wuhan-lr.aliyuncs.com
OSS_ACCESS_KEY_ID=<your-access-key-id>
OSS_ACCESS_KEY_SECRET=<your-access-key-secret>
OSS_BUCKET_NAME=vr-classroom-bucket

# JWT配置
JWT_SECRET=<your-jwt-secret-key>

# 服务器配置
SERVER_PORT=8080
```

#### .env.example模板
提供配置模板，指导团队成员设置本地开发环境，避免敏感信息泄露。

## 日志系统

### 日志配置

#### Logback配置 (logback-spring.xml)
- **控制台输出**：开发环境彩色日志
- **文件输出**：按环境分割日志文件
- **滚动策略**：按大小和时间滚动
- **错误日志**：单独的错误日志文件

#### 日志级别
- **开发环境**：DEBUG级别，详细日志
- **测试环境**：INFO级别，适中日志
- **生产环境**：WARN级别，精简日志

### 日志内容
- **请求日志**：记录所有API请求和响应
- **错误日志**：记录异常堆栈信息
- **业务日志**：记录关键业务操作（如创建帖子、用户登录）

## 项目架构

### 分层架构

```
Controller层 (控制器)
    ↓
Service层 (业务逻辑)
    ↓
Repository层 (数据访问)
    ↓
Database (数据库)
```

### 包结构
```
com.university.vrclassroombackend
├── config/              # 配置类
│   └── WebConfig.java
├── constant/            # 常量定义
│   └── AppConstants.java
├── controller/          # 控制器层
│   ├── UserController.java
│   ├── PostController.java
│   ├── CommentController.java
│   └── CommonController.java
├── dto/                # 数据传输对象
│   ├── ApiResponse.java
│   ├── LoginDTO.java
│   ├── PostCreateDTO.java
│   └── ...
├── interceptor/          # 拦截器
│   └── AuthInterceptor.java
├── model/              # 实体类
│   ├── User.java
│   ├── Post.java
│   ├── Comment.java
│   └── ...
├── repository/          # 数据访问层
│   ├── UserRepository.java
│   ├── PostRepository.java
│   └── ...
├── service/            # 服务层
│   ├── UserService.java
│   ├── PostService.java
│   └── impl/
│       ├── UserServiceImpl.java
│       └── ...
├── util/               # 工具类
│   ├── JwtUtil.java
│   └── OssUtil.java
└── vo/                 # 视图对象
    ├── UserProfileVO.java
    ├── PostVO.java
    └── ...
```

## API接口总览

### 用户相关接口
| 方法 | 路径 | 认证 | 功能描述 |
|------|------|--------|----------|
| POST | /api/users | 否 | 创建用户 |
| POST | /api/users/login | 否 | 用户登录 |
| GET | /api/users/profile | 是 | 获取用户信息 |
| GET | /api/users/posts | 是 | 获取用户帖子 |
| GET | /api/users/comments | 是 | 获取用户评论 |

### 帖子相关接口
| 方法 | 路径 | 认证 | 功能描述 |
|------|------|--------|----------|
| GET | /api/posts | 否 | 获取公开帖子列表 |
| POST | /api/posts | 是 | 创建帖子 |
| GET | /api/posts/{id} | 否 | 获取帖子详情 |
| PUT | /api/posts/{id} | 是 | 更新帖子 |
| DELETE | /api/posts/{id} | 是 | 删除帖子 |

### 评论相关接口
| 方法 | 路径 | 认证 | 功能描述 |
|------|------|--------|----------|
| GET | /api/comments | 否 | 获取帖子评论列表 |
| POST | /api/comments | 是 | 创建评论 |
| PUT | /api/comments/{id} | 是 | 更新评论 |
| DELETE | /api/comments/{id} | 是 | 删除评论 |

### 公共接口
| 方法 | 路径 | 认证 | 功能描述 |
|------|------|--------|----------|
| GET | /api/oss/sign | 可选 | 获取OSS上传签名 |
| GET | /health | 否 | 健康检查 |

## 常量定义

### AppConstants.java
```java
public class AppConstants {
    
    public static class HttpStatus {
        public static final int OK = 200;
        public static final int UNAUTHORIZED = 401;
        public static final int NOT_FOUND = 404;
        public static final int INTERNAL_SERVER_ERROR = 500;
    }
    
    public static class Auth {
        public static final String BEARER_PREFIX = "Bearer ";
        public static final String USER_ID_ATTRIBUTE = "userId";
    }
    
    public static class Pagination {
        public static final int DEFAULT_PAGE_SIZE = 20;
    }
    
    public static class Post {
        public static final int STATUS_DELETED = -1;
        public static final int STATUS_PENDING = 0;
        public static final int STATUS_PUBLISHED = 1;
        public static final int STATUS_REJECTED = 2;
        public static final int SUMMARY_LENGTH = 50;
        public static final int DEFAULT_CATEGORY_ID = 1;
    }
    
    public static class ErrorMessage {
        public static final String UNAUTHORIZED_USER = "未认证用户";
        public static final String TOKEN_NOT_PROVIDED = "未提供认证token";
        public static final String INVALID_TOKEN = "无效的token";
        public static final String USER_NOT_FOUND = "用户不存在";
        public static final String CREATE_POST_FAILED = "创建帖子失败";
        // ... 更多错误消息
    }
}
```

## 部署说明

### 本地开发
1. 配置环境变量（.env文件）
2. 启动MySQL数据库
3. 运行：`./mvnw spring-boot:run`
4. 访问：http://localhost:8080

### 生产部署
1. 设置系统环境变量
2. 构建项目：`./mvnw clean package`
3. 运行JAR：`java -jar target/vr-classroom-backend-0.0.1-SNAPSHOT.jar`
4. 激活生产环境：`--spring.profiles.active=prod`

### Docker部署（可选）
```dockerfile
FROM openjdk:17-jdk-slim
COPY target/vr-classroom-backend-0.0.1-SNAPSHOT.jar app.jar
ENTRYPOINT ["java","-jar","/app.jar"]
```

## 测试验证

### 测试脚本
- **test-api.py**：Python测试脚本，覆盖所有主要接口
- **测试结果**：8个测试用例全部通过

### 测试覆盖
- ✅ 用户登录和认证
- ✅ 帖子CRUD操作
- ✅ 评论CRUD操作
- ✅ OSS签名生成
- ✅ 分页查询
- ✅ 错误处理

## 项目亮点

### 1. 安全性
- JWT令牌认证机制
- 密钥长度自动补全
- 环境变量管理敏感信息
- Git忽略敏感文件

### 2. 可维护性
- 分层架构设计
- 统一的错误处理
- 完善的日志系统
- 环境差异化配置

### 3. 可扩展性
- 基于接口的服务层
- 灵活的配置系统
- 支持多种部署方式
- 模块化的包结构

### 4. 开发体验
- 热部署支持
- 详细的日志输出
- 清晰的代码结构
- 完善的API文档

## 后续优化建议

### 1. 功能增强
- 添加点赞功能接口
- 实现文件上传管理
- 增加搜索功能优化
- 支持用户关注功能

### 2. 性能优化
- 添加Redis缓存
- 数据库查询优化
- 接口响应时间监控
- 图片CDN加速

### 3. 安全增强
- 添加API限流
- 实现CSRF防护
- 增强输入验证
- 添加审计日志

### 4. 运维优化
- 容器化部署
- CI/CD流程集成
- 监控告警系统
- 自动化测试

## 总结

VR教室后端项目已完成核心功能开发，实现了用户管理、帖子管理、评论管理等主要功能模块。项目采用现代化的技术栈，具有良好的架构设计和代码质量。通过完善的配置系统和日志机制，支持多环境部署。JWT认证机制确保了API的安全性，OSS集成提供了文件存储能力。项目整体结构清晰，代码规范，为后续功能扩展和维护奠定了良好基础。
